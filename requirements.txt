# Core dataset and manuscript-reproduction environment (Python 3.11)
#
# NumPy is deliberately constrained below 2.0 because the OpenCV wheel used
# for the frozen CleanCam v2 analyses targets the NumPy 1.x ABI.
numpy==1.26.4
pandas==2.2.3
opencv-python-headless==4.10.0.84
Pillow==10.4.0
matplotlib==3.9.2
scikit-learn==1.5.2
scipy==1.14.1
tqdm==4.66.5
